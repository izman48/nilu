# Database Configuration
DATABASE_URL=postgresql://user:password@db:5432/nilu_tourism

# Security
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application
PROJECT_NAME=Nilu Tourism Platform
VERSION=1.0.0
API_V1_STR=/api/v1
ENVIRONMENT=development

# File Upload Settings
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE=10485760  # 10MB in bytes
# Allowed file extensions (comma separated): .jpg,.jpeg,.png,.pdf

# CORS Origins (comma separated)
# BACKEND_CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# SMS Configuration - Twilio (Optional)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ============================================
# Firebase Storage Configuration (OPTIONAL)
# ============================================
# If you want to use Firebase Storage instead of local storage,
# follow these steps:

# 1. Create a Firebase project at https://console.firebase.google.com
# 2. Go to Project Settings > Service Accounts
# 3. Click "Generate New Private Key" to download the credentials JSON file
# 4. Save the JSON file to a secure location (e.g., /app/firebase-credentials.json in Docker)
# 5. Enable Firebase Storage in your Firebase project
# 6. Set the following environment variables:

USE_FIREBASE_STORAGE=false  # Set to 'true' to enable Firebase Storage
FIREBASE_CREDENTIALS_PATH=/path/to/firebase-service-account.json
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com

# Notes:
# - If USE_FIREBASE_STORAGE=false or not set, files will be stored locally in UPLOAD_DIR
# - If Firebase credentials are invalid/missing, the system will automatically fall back to local storage
# - Firebase URLs are public and work directly from the browser
# - Local files are served via /uploads endpoint
